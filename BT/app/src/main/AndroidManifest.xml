<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuDemoActivity"></activity>
        <activity android:name=".ex.DrawbleActivity" />
        <activity android:name=".ex.App" />
        <activity android:name=".ex.ProBars" />
        <activity android:name=".ex.CheckWF" />
        <activity android:name=".GridView.EditText" />
        <activity android:name=".ex.Run" />
        <activity android:name=".ex.CheckBox" />
        <activity android:name=".ex.ButtonCustomActivity" />
        <activity android:name=".ListView.ImgView" />
        <activity android:name=".CustomListView.custum" />
        <activity android:name=".GridView.GView2" />
        <activity android:name=".GridView.GridView1" />
        <activity android:name=".ListView.ListView1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>